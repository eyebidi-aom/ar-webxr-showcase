# AR 3D Showcase

เว็บแอป Augmented Reality (AR) ที่แสดงผลโมเดล 3 มิติบนพื้นจริงผ่านกล้องมือถือ สร้างด้วย [A-Frame](https://aframe.io) และ WebXR Hit-Test API พร้อมฟีเจอร์แนบวิดีโอ กราฟ และสไลด์ประกอบ พร้อมอินเตอร์แอคทีฟหมุน/ซูมโมเดลได้แบบเรียลไทม์

🔗 **เปิดใช้งาน:** เมื่อเปิดใช้งาน GitHub Pages แล้ว จะเข้าถึงได้ที่ `https://<username>.github.io/<repo-name>/`

## ฟีเจอร์

- 📷 **AR วางบนพื้นจริง (Markerless)** — ใช้ WebXR Hit-Test ตรวจจับพื้นแล้ววางโมเดล 3 มิติได้โดยไม่ต้องใช้มาร์กเกอร์
- 🖐️ **อินเตอร์แอคทีฟ** — ลากนิ้วเพื่อหมุนโมเดล บีบสองนิ้วเพื่อซูม หรือใช้ปุ่มควบคุมในแถบเครื่องมือ
- 🎥 **แนบวิดีโอ** — เปิด/ปิดแผ่นวิดีโอลอยข้างโมเดลได้ในหนึ่งแตะ
- 📊 **แนบกราฟ** — แสดงกราฟตัวอย่าง (Chart.js) เป็นพื้นผิวลอยในฉาก AR
- 🖼️ **แนบสไลด์** — สไลด์โชว์ภาพประกอบ เลื่อนหน้าก่อน/ถัดไปได้
- 🧊 **โหมดพรีวิว 3 มิติ** — สำหรับอุปกรณ์ที่ไม่รองรับ WebXR AR (เช่น iPhone) ดูโมเดลแบบหมุนได้โดยไม่ต้องใช้กล้อง
- 🎨 UI แบบ Glassmorphism ทันสมัย รองรับภาษาไทย (ฟอนต์ Kanit)

## ข้อกำหนดของอุปกรณ์

โหมด AR เต็มรูปแบบต้องใช้:

- **Chrome บน Android** ที่รองรับ [WebXR Device API](https://developer.mozilla.org/en-US/docs/Web/API/WebXR_Device_API) (ARCore)
- การเชื่อมต่อผ่าน **HTTPS** (WebXR/กล้องจะไม่ทำงานบน HTTP ธรรมดา — GitHub Pages เป็น HTTPS อยู่แล้ว)
- อนุญาตสิทธิ์การใช้กล้องเมื่อเบราว์เซอร์ขอ

หากอุปกรณ์ไม่รองรับ (เช่น iPhone/Safari หรือคอมพิวเตอร์เดสก์ท็อป) เว็บจะสลับไปที่โหมด **"ดูตัวอย่าง 3 มิติ (ไม่ใช้กล้อง)"** ให้อัตโนมัติ

## วิธีใช้งาน

1. เปิดลิงก์เว็บไซต์บนมือถือ (Chrome/Android แนะนำ)
2. กด **"เริ่มประสบการณ์ AR"** แล้วอนุญาตให้ใช้กล้อง
3. เลื่อนกล้องช้าๆ ไปทางพื้น จนกว่าจะเห็นวงแหวนสีฟ้า (แสดงว่าพบพื้นแล้ว)
4. แตะปุ่ม **📍 วางโมเดล**
5. ใช้นิ้วลาก/บีบเพื่อหมุนและซูมโมเดล หรือใช้ปุ่มในแถบเครื่องมือด้านล่าง
6. แตะ **🎥 วิดีโอ**, **📊 กราฟ**, **🖼️ สไลด์** เพื่อเปิด/ปิดสื่อประกอบรอบโมเดล
7. แตะ **♻️ รีเซ็ต** เพื่อเริ่มวางใหม่ หรือ **✕** เพื่อออกจากโหมด AR

## การปรับแต่ง

ไฟล์ทั้งหมดอยู่ใน `index.html` ไฟล์เดียว (ไม่ต้อง build):

- **เปลี่ยนโมเดล 3 มิติ**: แก้ `src` ของ `<a-asset-item id="demoModel">` ให้ชี้ไปที่ไฟล์ `.glb`/`.gltf` ของคุณ (ต้องเป็น URL ที่รองรับ CORS)
- **เปลี่ยนวิดีโอ**: แก้ `src` ของ `<video id="demoVideo">`
- **เปลี่ยนข้อมูลกราฟ**: แก้ในฟังก์ชัน `buildChart()` (ใช้ Chart.js)
- **เปลี่ยนสไลด์**: แก้ array `slides` ในสคริปต์ท้ายไฟล์

โมเดลตัวอย่าง (Astronaut) และวิดีโอตัวอย่างในไฟล์นี้เป็นไฟล์สาธารณะที่ใช้สำหรับ demo เท่านั้น แนะนำให้เปลี่ยนเป็นไฟล์ของคุณเองก่อนใช้งานจริง

## เทคโนโลยีที่ใช้

- [A-Frame 1.5](https://aframe.io/) — WebVR/WebXR framework
- [WebXR Hit-Test API](https://immersive-web.github.io/hit-test/) (ผ่าน component `ar-hit-test` ของ A-Frame) — ตรวจจับพื้นแบบ markerless
- [Chart.js](https://www.chartjs.org/) — เรนเดอร์กราฟเป็นพื้นผิว (texture) ในฉาก 3 มิติ
- WebXR DOM Overlay — แสดง UI ปุ่มควบคุมทับบนภาพกล้องจริง

> หมายเหตุ: การวางโมเดลบนพื้นแบบ markerless (ไม่ใช้มาร์กเกอร์ภาพ) ใช้ความสามารถ WebXR Hit-Test ของเบราว์เซอร์โดยตรง ซึ่งรองรับเฉพาะ Chrome บน Android ปัจจุบันไลบรารี AR.js (แบบมาร์กเกอร์/ตำแหน่ง GPS) ยังไม่รองรับการตรวจจับพื้นแบบนี้ จึงเลือกใช้ความสามารถ WebXR ในตัวของ A-Frame แทนสำหรับโหมดนี้
